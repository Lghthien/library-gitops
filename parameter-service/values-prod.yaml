replicaCount: 2

image:
  repository: legiahoangthien/library-parameter-service
  tag: "prod-initial"

service:
  type: ClusterIP
  port: 4007

needsDatabase: true

env:
  NODE_ENV: "production"
  PORT: "4007"
