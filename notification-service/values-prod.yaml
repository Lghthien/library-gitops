replicaCount: 2

image:
  repository: lghthien/library-notification-service
  tag: "prod-initial"

service:
  type: ClusterIP
  port: 4004

needsDatabase: true

env:
  NODE_ENV: "production"
  PORT: "4004"
