replicaCount: 2

image:
  repository: lghthien/library-reader-service
  tag: "prod-initial"

service:
  type: ClusterIP
  port: 4005

needsDatabase: true

env:
  NODE_ENV: "production"
  PORT: "4005"
